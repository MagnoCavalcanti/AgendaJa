// Configuração do PostCSS, processador de CSS usado pelo Tailwind.
module.exports = {
  plugins: {
    tailwindcss: {},   // Plugin que gera as classes utilitárias do Tailwind.
    autoprefixer: {},  // Adiciona prefixos de navegador automaticamente (-webkit-, etc.).
  },
};
